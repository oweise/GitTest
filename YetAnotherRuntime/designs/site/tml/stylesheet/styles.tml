<tml:script>
	// We don't want CSS-Requests to appear in access log statistics:
    var info = request.getAttribute(Packages.de.innovationgate.wgpublisher.log.WGARequestInformation.REQUEST_ATTRIBUTENAME);
    info.setLoggingEnabled(false);
</tml:script>

<tml:include ref="styles@base"/>
